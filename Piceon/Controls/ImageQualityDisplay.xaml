<UserControl
    x:Class="Piceon.Controls.ImageQualityDisplay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Piceon.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" >

    <Grid PointerEntered="UserControl_PointerEntered"
          PointerExited="UserControl_PointerExited" >
        <Grid.Background>
            <SolidColorBrush Color="Black" Opacity="0" />
        </Grid.Background>
        <Grid Height="{Binding Height , ElementName=textBlock}"
              Width="{Binding Width, ElementName=textBlock}"
              HorizontalAlignment="Left"
              VerticalAlignment="Bottom">
            <!--<Grid.Background>
                <SolidColorBrush Color="Black" Opacity="0.5" />
            </Grid.Background>-->
            <TextBlock x:Name="textBlock"
                       Margin="8,0,5,3"
                       Visibility="Collapsed"/>
        </Grid>
    </Grid>
</UserControl>
